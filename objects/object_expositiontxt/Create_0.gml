//text itself
etext = "Hello, losers."; //placeholder of sorts

//for text array
epage = 0;
echarCount = 0;
ename = noone;

//box for textbox
eboxWidth = sprite_get_width(spr_expotxt);
eboxHeight = sprite_get_height(spr_expotxt);
estringHeight = string_height(etext);

//go to next room
finished = false;

